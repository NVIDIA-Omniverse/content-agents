interface:
  display_name: "Material Agent CLI"
  short_description: "Run the Material Agent CLI"
  default_prompt: "Use $material-agent-cli to run the Material Agent CLI for VLM-based material assignment to 3D objects."
